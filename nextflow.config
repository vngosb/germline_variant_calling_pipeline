docker.enabled = true
docker.runOptions = '--platform linux/amd64'

profiles {
    test {
        params.reads = "${projectDir}/data/*_{1,2}.fastq.gz"
        params.genome = "${projectDir}/reference/genome*"
    }
}
process {
  cpus = 4
}